{
  "name": "bugbase",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "publish:cli": "npm publish --workspace @monodox/bugbase --access public",
    "publish:mcp": "npm publish --workspace @monodox/bugbase-mcp-server --access public"
  }
}
